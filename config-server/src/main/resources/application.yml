spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/cbuelvasc/microservices/blob/master/config/items-service.yml

server:
  port: 8888

configserver:
  name: Registry
  status: Connected to the Registry